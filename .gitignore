# Bí mật — TUYỆT ĐỐI không commit, không gửi cho ai
.env

# Rác Python / Jupyter
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.venv/
venv/
.pytest_cache/

# Dữ liệu không vào git — xem notebook/data/.gitignore.
# Gửi cộng sự qua Drive/USB, không qua git.

# Bản lưu v1 (2,6 GB) — chỉ để đối chiếu, dựng lại được từ raw
/v1_backup/

# Log của các luồng tải — chỉ có ý nghĩa trên máy đang chạy
logs/

# Kết quả mô hình (Stage 2 trở đi)
analysis/

# Output kiểm chứng API, tái tạo bằng notebook/scripts/wits_probe.py.
# Giữ lại đúng nhật ký 22 phép thử vì tài liệu KIEM_CHUNG có trích dẫn.
notebook/probe_out/*
!notebook/probe_out/_probe_log.csv

# Rác hệ điều hành / trình soạn thảo
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Báo cáo kiểm định của từng máy (bước 30 notebook). Bản tham chiếu là stage1_v2.*
notebook/docs/audit/stage1_local*

/wits_notebook_raw_20260922

